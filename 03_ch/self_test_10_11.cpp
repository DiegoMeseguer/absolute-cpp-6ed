#include <iostream>
using namespace std;

int main() {

	cout<<"Answer in the textbook"<<endl;
	cout<<"Answer in the textbook"<<endl;

	/*
	10) No, a function definition cannot appear inside the body
		of another function definition.

	11) You call both types of functions the same way. In the case of the
		predefined functions you have to include a directive at the start
		of your program for their libraries. With the user-defined functions
		you just need to make sure that you have declared and defined them before
		you use them.
	*/

	return 0;
}
