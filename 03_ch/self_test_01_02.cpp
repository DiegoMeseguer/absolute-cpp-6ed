#include<iostream>
#include<cmath>
using namespace std;

int main() {

	cout<<"Answer in the textbook"<<endl;
	cout<<"Answer in the textbook"<<endl;

	/*
	1) 
	4.0
	8
	3
	3.0
	6.0
	5.0
	3
	4.0
	8.0
	3
	3.5
	6
	4.5
	3.0
	8.0
	1.21
	0
	3.5
	5.0
	4.5
	3.0

	2)
	a) sqrt((x + y))
	b) pow(x, (y + 7))
	c) sqrt((area + fudge))
	d) sqrt((time + tide)) / nobody
	e)	(-b + sqrt((pow(b, 2) - (4 * a * c))) / (2 * a)
		(-b - sqrt((pow(b, 2) - (4 * a * c))) / (2 * a)
	f) abs((x - y))
	*/


	return 0;
}
