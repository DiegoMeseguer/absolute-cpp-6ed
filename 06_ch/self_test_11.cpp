#include <iostream>
#include <string>
using namespace std;

int main() {
	

/*
	11) Both operators specify to what thing a member name is part of.
		In the case of the dot (.) operator, it tells us that the 
		member variable or function to the right of the dot belongs to the
		object on the left side of the dot. In the case of the scope
		resolution operator (::), it tells us that the member function to the
		right of the colons belongs to the class name on the left side of the
		colons.
*/

	return 0;
}
