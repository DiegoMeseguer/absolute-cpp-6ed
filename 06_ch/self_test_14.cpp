#include <iostream>
#include <string>
using namespace std;

int main() {
	
/*
	14) Private means that the member variables and functions cannot
		be referenced outside the class (more specifically "they
		cannot be referenced by name anyplace except within the
		definitions of the member functions of the class")

		Public means that the member variables and functions can be
		used anywhere.
*/

	return 0;
}
