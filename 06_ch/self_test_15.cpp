#include <iostream>
#include <string>
using namespace std;

int main() {
	
/*
	15)

	a) At least one Public section, so there are accessor and
	mutator functions that can be used to interact with the 
	class objects.

	b) Strictly speaking no private section is required. But
	for good programming practices there should be at least
	one private section for the member variables in order to
	enforce the principle of encapsulation.
*/

	return 0;
}
