#include <iostream>
#include <string>
using namespace std;

int main() {
	
	// Because they aren't the same, since the first one is
	// a character array of 4 elements whose last element 
	// is '\0' and the last one is a character array of three 
	// elements: 'a', 'b' and 'c'.

	return 0;
}
