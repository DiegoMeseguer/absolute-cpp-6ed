#include <iostream>
#include <string>
using namespace std;

int main() {
	
	// a) If the c-string variable is malformed (the '\0' is
	// 	missing) then it will continue iterating beyond
	// 	the array boundaries.
	// b) We just need to make the following change
	// 	while(ourString[index] != '\0' && index < SIZE)

	return 0;
}
