#include <iostream>
using namespace std;

int main() {

	cout<<"Answer in the textbook"<<endl;

	/*
	1) In the case of call-by-value, only the value of the argument is plugged into the
	function. So we don't have to worry about the value of the variable, that we used as
	argument, changing.
	*/

	return 0;
}
