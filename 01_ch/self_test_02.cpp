#include<iostream>
using namespace std;

int main() {

	int count(0);
	double distance(1.5);

	// Alternative way to initialize a variable in C++

	count = 0;
	distance = 1.5;

	cout<<"The value of count is: "<<count<<"\n"
		<<"The value of distance is: "<< distance<<endl;

	return 0;
}
