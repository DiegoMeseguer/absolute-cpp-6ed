#include <iostream>
#include <string>
using namespace std;

int main() {
	
	// 8) No, because the function showStatus uses the
	// 	the  non-static variable "name" and this means
	// 	that it relies on calling an object.

	return 0;
}
