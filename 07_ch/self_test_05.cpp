#include <iostream>
#include <string>
using namespace std;

int main() {
	
	// 5) The similarity is that in both cases the variable
	// that we are giving to the function is not going to
	// be modified. The difference is that call by value
	// is more expensive because it creates a copy, while
	// const parameters consume less memory. Also const parameters
	// force us to adopt an "all or nothing" approach.
	
	return 0;
}
